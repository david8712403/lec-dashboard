{
  "name": "lec-dashboard",
  "private": true,
  "workspaces": ["web", "server"],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm --workspace web run dev\" \"npm --workspace server run dev\"",
    "dev:web": "npm --workspace web run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace web run build && npm --workspace server run build",
    "prisma:generate": "npm --workspace server run prisma:generate",
    "prisma:migrate": "npm --workspace server run prisma:migrate",
    "prisma:seed": "npm --workspace server run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
